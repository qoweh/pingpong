name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Deploy
        run: |
            cd /home/pilt/pingpong

            git fetch origin
            git reset --hard origin/main

            docker compose up -d --build --force-recreate